{
  "name": "farm-evolution",
  "version": "1.0.0",
  "description": "Farm Evolution — pixel-art merge/idle farm game",
  "private": true,
  "scripts": {
    "build:www": "node scripts/build-www.js",
    "build:single": "node scripts/build-single.js",
    "android:sync": "npm run build:www && npx cap sync android",
    "android:apk": "npm run android:sync && cd android && ./gradlew assembleDebug"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0"
  }
}
